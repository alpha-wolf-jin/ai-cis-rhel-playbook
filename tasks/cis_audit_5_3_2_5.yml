---
- name: Initialize meta data variables
  set_fact:
    cis_reference: CIS RHEL 9 Benchmark v2.0.0 checkpoint 5.3.2.5
    req_1: Check that pam_unix.so is present in the auth section of /etc/pam.d/password-auth
    req_2: Check that pam_unix.so is present in the account section of /etc/pam.d/password-auth
    req_3: Check that pam_unix.so is present in the password section of /etc/pam.d/password-auth
    req_4: Check that pam_unix.so is present in the session section of /etc/pam.d/password-auth
    req_5: Check that pam_unix.so is present in the auth section of /etc/pam.d/system-auth
    req_6: Check that pam_unix.so is present in the account section of /etc/pam.d/system-auth
    req_7: Check that pam_unix.so is present in the password section of /etc/pam.d/system-auth
    req_8: Check that pam_unix.so is present in the session section of /etc/pam.d/system-auth
    req_9: 'OVERALL Verify: Ensure pam_unix module is enabled'

- name: Initialize data variables
  set_fact:
    task_1_name: "Not executed"
    task_1_cmd: "N/A"
    task_1_rc: -1
    task_2_name: "Not executed"
    task_2_cmd: "N/A"
    task_2_rc: -1
    task_3_name: "Not executed"
    task_3_cmd: "N/A"
    task_3_rc: -1
    task_4_name: "Not executed"
    task_4_cmd: "N/A"
    task_4_rc: -1
    task_5_name: "Not executed"
    task_5_cmd: "N/A"
    task_5_rc: -1
    task_6_name: "Not executed"
    task_6_cmd: "N/A"
    task_6_rc: -1
    task_7_name: "Not executed"
    task_7_cmd: "N/A"
    task_7_rc: -1
    task_8_name: "Not executed"
    task_8_cmd: "N/A"
    task_8_rc: -1
    task_9_name: "Not executed"
    task_9_cmd: "N/A"
    task_9_rc: -1

    # CIS Audit Procedure Command (kept as additional check)
- name: Req 9 - Execute CIS audit procedure command for pam_unix.so
  shell: |
    grep -P -- '\bpam_unix\.so\b' /etc/pam.d/{password,system}-auth
  args:
    executable: /bin/bash
  register: audit_procedure_output
  ignore_errors: true
  changed_when: false

    # Requirement 1: Check auth section in password-auth
- name: Req 1 - Check auth section in password-auth
  shell: |
    grep -P -- '^auth\s+.*pam_unix\.so' /etc/pam.d/password-auth
  args:
    executable: /bin/bash
  register: result_1
  ignore_errors: true
  changed_when: false

    # Requirement 2: Check account section in password-auth
- name: Req 2 - Check account section in password-auth
  shell: |
    grep -P -- '^account\s+.*pam_unix\.so' /etc/pam.d/password-auth
  args:
    executable: /bin/bash
  register: result_2
  ignore_errors: true
  changed_when: false

    # Requirement 3: Check password section in password-auth
- name: Req 3 - Check password section in password-auth
  shell: |
    grep -P -- '^password\s+.*pam_unix\.so' /etc/pam.d/password-auth
  args:
    executable: /bin/bash
  register: result_3
  ignore_errors: true
  changed_when: false

    # Requirement 4: Check session section in password-auth
- name: Req 4 - Check session section in password-auth
  shell: |
    grep -P -- '^session\s+.*pam_unix\.so' /etc/pam.d/password-auth
  args:
    executable: /bin/bash
  register: result_4
  ignore_errors: true
  changed_when: false

    # Requirement 5: Check auth section in system-auth
- name: Req 5 - Check auth section in system-auth
  shell: |
    grep -P -- '^auth\s+.*pam_unix\.so' /etc/pam.d/system-auth
  args:
    executable: /bin/bash
  register: result_5
  ignore_errors: true
  changed_when: false

    # Requirement 6: Check account section in system-auth
- name: Req 6 - Check account section in system-auth
  shell: |
    grep -P -- '^account\s+.*pam_unix\.so' /etc/pam.d/system-auth
  args:
    executable: /bin/bash
  register: result_6
  ignore_errors: true
  changed_when: false

    # Requirement 7: Check password section in system-auth
- name: Req 7 - Check password section in system-auth
  shell: |
    grep -P -- '^password\s+.*pam_unix\.so' /etc/pam.d/system-auth
  args:
    executable: /bin/bash
  register: result_7
  ignore_errors: true
  changed_when: false

    # Requirement 8: Check session section in system-auth
- name: Req 8 - Check session section in system-auth
  shell: |
    grep -P -- '^session\s+.*pam_unix\.so' /etc/pam.d/system-auth
  args:
    executable: /bin/bash
  register: result_8
  ignore_errors: true
  changed_when: false

    # Store requirement 1 details
- name: Store requirement 1 details
  set_fact:
    task_1_name: "Check auth section in password-auth"
    task_1_cmd: |
      grep -P -- '^auth\s+.*pam_unix\.so' /etc/pam.d/password-auth
    task_1_rc: "{{ result_1.rc | default(-1) }}"
    data_1: "{{ result_1.stdout | default('') | trim }}"
    status_1: "{{ ('PASS' if (result_1.stdout | default('') | trim != '') else 'FAIL') | trim }}"
    rationale_1: "{{ ('PASS when the line exists and contains pam_unix.so, FAIL otherwise' if (result_1.stdout | default('') | trim != '') else 'FAIL: No matching line found') | trim }}"

    # Store requirement 2 details
- name: Store requirement 2 details
  set_fact:
    task_2_name: "Check account section in password-auth"
    task_2_cmd: |
      grep -P -- '^account\s+.*pam_unix\.so' /etc/pam.d/password-auth
    task_2_rc: "{{ result_2.rc | default(-1) }}"
    data_2: "{{ result_2.stdout | default('') | trim }}"
    status_2: "{{ ('PASS' if (result_2.stdout | default('') | trim != '') else 'FAIL') | trim }}"
    rationale_2: "{{ ('PASS when the line exists and contains pam_unix.so, FAIL otherwise' if (result_2.stdout | default('') | trim != '') else 'FAIL: No matching line found') | trim }}"

    # Store requirement 3 details
- name: Store requirement 3 details
  set_fact:
    task_3_name: "Check password section in password-auth"
    task_3_cmd: |
      grep -P -- '^password\s+.*pam_unix\.so' /etc/pam.d/password-auth
    task_3_rc: "{{ result_3.rc | default(-1) }}"
    data_3: "{{ result_3.stdout | default('') | trim }}"
    status_3: "{{ ('PASS' if (result_3.stdout | default('') | trim != '') else 'FAIL') | trim }}"
    rationale_3: "{{ ('PASS when the line exists and contains pam_unix.so, FAIL otherwise' if (result_3.stdout | default('') | trim != '') else 'FAIL: No matching line found') | trim }}"

    # Store requirement 4 details
- name: Store requirement 4 details
  set_fact:
    task_4_name: "Check session section in password-auth"
    task_4_cmd: |
      grep -P -- '^session\s+.*pam_unix\.so' /etc/pam.d/password-auth
    task_4_rc: "{{ result_4.rc | default(-1) }}"
    data_4: "{{ result_4.stdout | default('') | trim }}"
    status_4: "{{ ('PASS' if (result_4.stdout | default('') | trim != '') else 'FAIL') | trim }}"
    rationale_4: "{{ ('PASS when the line exists and contains pam_unix.so, FAIL otherwise' if (result_4.stdout | default('') | trim != '') else 'FAIL: No matching line found') | trim }}"

    # Store requirement 5 details
- name: Store requirement 5 details
  set_fact:
    task_5_name: "Check auth section in system-auth"
    task_5_cmd: |
      grep -P -- '^auth\s+.*pam_unix\.so' /etc/pam.d/system-auth
    task_5_rc: "{{ result_5.rc | default(-1) }}"
    data_5: "{{ result_5.stdout | default('') | trim }}"
    status_5: "{{ ('PASS' if (result_5.stdout | default('') | trim != '') else 'FAIL') | trim }}"
    rationale_5: "{{ ('PASS when the line exists and contains pam_unix.so, FAIL otherwise' if (result_5.stdout | default('') | trim != '') else 'FAIL: No matching line found') | trim }}"

    # Store requirement 6 details
- name: Store requirement 6 details
  set_fact:
    task_6_name: "Check account section in system-auth"
    task_6_cmd: |
      grep -P -- '^account\s+.*pam_unix\.so' /etc/pam.d/system-auth
    task_6_rc: "{{ result_6.rc | default(-1) }}"
    data_6: "{{ result_6.stdout | default('') | trim }}"
    status_6: "{{ ('PASS' if (result_6.stdout | default('') | trim != '') else 'FAIL') | trim }}"
    rationale_6: "{{ ('PASS when the line exists and contains pam_unix.so, FAIL otherwise' if (result_6.stdout | default('') | trim != '') else 'FAIL: No matching line found') | trim }}"

    # Store requirement 7 details
- name: Store requirement 7 details
  set_fact:
    task_7_name: "Check password section in system-auth"
    task_7_cmd: |
      grep -P -- '^password\s+.*pam_unix\.so' /etc/pam.d/system-auth
    task_7_rc: "{{ result_7.rc | default(-1) }}"
    data_7: "{{ result_7.stdout | default('') | trim }}"
    status_7: "{{ ('PASS' if (result_7.stdout | default('') | trim != '') else 'FAIL') | trim }}"
    rationale_7: "{{ ('PASS when the line exists and contains pam_unix.so, FAIL otherwise' if (result_7.stdout | default('') | trim != '') else 'FAIL: No matching line found') | trim }}"

    # Store requirement 8 details
- name: Store requirement 8 details
  set_fact:
    task_8_name: "Check session section in system-auth"
    task_8_cmd: |
      grep -P -- '^session\s+.*pam_unix\.so' /etc/pam.d/system-auth
    task_8_rc: "{{ result_8.rc | default(-1) }}"
    data_8: "{{ result_8.stdout | default('') | trim }}"
    status_8: "{{ ('PASS' if (result_8.stdout | default('') | trim != '') else 'FAIL') | trim }}"
    rationale_8: "{{ ('PASS when the line exists and contains pam_unix.so, FAIL otherwise' if (result_8.stdout | default('') | trim != '') else 'FAIL: No matching line found') | trim }}"

    # Requirement 9: Overall verification
- name: Store requirement 9 details
  set_fact:
    task_9_name: "Overall verification"
    task_9_cmd: |
      grep -P -- '\bpam_unix\.so\b' /etc/pam.d/{password,system}-auth
    task_9_rc: "{{ audit_procedure_output.rc | default(-1) }}"
    data_9: "{{ audit_procedure_output.stdout | default('') | trim }}"
    status_9: "{{ ('PASS' if (status_1|trim == 'PASS' and status_2|trim == 'PASS' and status_3|trim == 'PASS' and status_4|trim == 'PASS' and status_5|trim == 'PASS' and status_6|trim == 'PASS' and status_7|trim == 'PASS' and status_8|trim == 'PASS') else 'FAIL') | trim }}"
    rationale_9: "{{ ('PASS when req_1=PASS AND req_2=PASS AND req_3=PASS AND req_4=PASS AND req_5=PASS AND req_6=PASS AND req_7=PASS AND req_8=PASS, FAIL otherwise' if (status_1|trim == 'PASS' and status_2|trim == 'PASS' and status_3|trim == 'PASS' and status_4|trim == 'PASS' and status_5|trim == 'PASS' and status_6|trim == 'PASS' and status_7|trim == 'PASS' and status_8|trim == 'PASS') else 'FAIL: One or more requirements failed') | trim }}"

- name: Generate compliance report
  debug:
    msg:
    - "========================================================"
    - "        COMPLIANCE REPORT - CIS 5.3.2.5"
    - "========================================================"
    - "Reference: {{ cis_reference }}"
    - "========================================================"
    - ""
    - "REQUIREMENT 1 - {{ req_1 }}:"
    - "  Task: {{ task_1_name | default('Task not recorded') }}"
    - "  Command: {{ task_1_cmd | default('N/A') }}"
    - "  Exit code: {{ task_1_rc | default(-1) }}"
    - "  Data: {{ data_1 | default('') | trim }}"
    - "  Status: {{ status_1 | default('UNKNOWN') | trim }}"
    - "  Rationale: {{ rationale_1 | default('Not evaluated') | trim }}"
    - ""
    - "REQUIREMENT 2 - {{ req_2 }}:"
    - "  Task: {{ task_2_name | default('Task not recorded') }}"
    - "  Command: {{ task_2_cmd | default('N/A') }}"
    - "  Exit code: {{ task_2_rc | default(-1) }}"
    - "  Data: {{ data_2 | default('') | trim }}"
    - "  Status: {{ status_2 | default('UNKNOWN') | trim }}"
    - "  Rationale: {{ rationale_2 | default('Not evaluated') | trim }}"
    - ""
    - "REQUIREMENT 3 - {{ req_3 }}:"
    - "  Task: {{ task_3_name | default('Task not recorded') }}"
    - "  Command: {{ task_3_cmd | default('N/A') }}"
    - "  Exit code: {{ task_3_rc | default(-1) }}"
    - "  Data: {{ data_3 | default('') | trim }}"
    - "  Status: {{ status_3 | default('UNKNOWN') | trim }}"
    - "  Rationale: {{ rationale_3 | default('Not evaluated') | trim }}"
    - ""
    - "REQUIREMENT 4 - {{ req_4 }}:"
    - "  Task: {{ task_4_name | default('Task not recorded') }}"
    - "  Command: {{ task_4_cmd | default('N/A') }}"
    - "  Exit code: {{ task_4_rc | default(-1) }}"
    - "  Data: {{ data_4 | default('') | trim }}"
    - "  Status: {{ status_4 | default('UNKNOWN') | trim }}"
    - "  Rationale: {{ rationale_4 | default('Not evaluated') | trim }}"
    - ""
    - "REQUIREMENT 5 - {{ req_5 }}:"
    - "  Task: {{ task_5_name | default('Task not recorded') }}"
    - "  Command: {{ task_5_cmd | default('N/A') }}"
    - "  Exit code: {{ task_5_rc | default(-1) }}"
    - "  Data: {{ data_5 | default('') | trim }}"
    - "  Status: {{ status_5 | default('UNKNOWN') | trim }}"
    - "  Rationale: {{ rationale_5 | default('Not evaluated') | trim }}"
    - ""
    - "REQUIREMENT 6 - {{ req_6 }}:"
    - "  Task: {{ task_6_name | default('Task not recorded') }}"
    - "  Command: {{ task_6_cmd | default('N/A') }}"
    - "  Exit code: {{ task_6_rc | default(-1) }}"
    - "  Data: {{ data_6 | default('') | trim }}"
    - "  Status: {{ status_6 | default('UNKNOWN') | trim }}"
    - "  Rationale: {{ rationale_6 | default('Not evaluated') | trim }}"
    - ""
    - "REQUIREMENT 7 - {{ req_7 }}:"
    - "  Task: {{ task_7_name | default('Task not recorded') }}"
    - "  Command: {{ task_7_cmd | default('N/A') }}"
    - "  Exit code: {{ task_7_rc | default(-1) }}"
    - "  Data: {{ data_7 | default('') | trim }}"
    - "  Status: {{ status_7 | default('UNKNOWN') | trim }}"
    - "  Rationale: {{ rationale_7 | default('Not evaluated') | trim }}"
    - ""
    - "REQUIREMENT 8 - {{ req_8 }}:"
    - "  Task: {{ task_8_name | default('Task not recorded') }}"
    - "  Command: {{ task_8_cmd | default('N/A') }}"
    - "  Exit code: {{ task_8_rc | default(-1) }}"
    - "  Data: {{ data_8 | default('') | trim }}"
    - "  Status: {{ status_8 | default('UNKNOWN') | trim }}"
    - "  Rationale: {{ rationale_8 | default('Not evaluated') | trim }}"
    - ""
    - "REQUIREMENT 9 - {{ req_9 }}:"
    - "  Task: {{ task_9_name | default('Task not recorded') }}"
    - "  Command: {{ task_9_cmd | default('N/A') }}"
    - "  Exit code: {{ task_9_rc | default(-1) }}"
    - "  Data: {{ data_9 | default('') | trim }}"
    - "  Status: {{ status_9 | default('UNKNOWN') | trim }}"
    - "  Rationale: {{ rationale_9 | default('Not evaluated') | trim }}"
    - ""
    - "========================================================"
    - "OVERALL COMPLIANCE:"
    - "  Result: {{ status_9 | trim }}"
    - "  Rationale: {{ rationale_9 | trim }}"
    - "========================================================"

- name: Set HTML Report Params
  set_fact:
    audit_result: '{{ audit_result | default([]) + [{ ''status_5_3_2_5'': status_9 | trim }] }}'
