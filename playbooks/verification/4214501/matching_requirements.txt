================================================================================
MATCHING REQUIREMENTS
KCS Article: systemd stops reading and processing dbus event of runc cgroup invokes
URL: https://access.redhat.com/solutions/4214501
================================================================================

1. Measure if OS version matches: Red Hat Enterprise Linux 7

2. Measure if the environment is Openshift Container Platform 3.x

3. Measure if the systemd service is active and running

4. Measure if journal logs contain the error message: 'Failed to propagate agent release message: Operation not supported'

5. Measure if journal logs contain the error message: 'Failed to start user slice' or 'Failed to start session scope' with 'Connection timed out' from systemd-logind

6. Measure if pods are reported in 'ContainerCreating' or 'Terminating' state for an extended period (e.g., more than a day) when querying the node's pod status

7. Measure if docker or containerd operations fail with error messages indicating connection or timeout issues, such as 'container did not start before the specified timeout' or 'The maximum number of active connections for UID 0 has been reached'

8. Measure if the atomic-openshift-node service is present and its logs contain warnings like 'FailedCreatePodSandBox' with related runtime errors

9. Measure if journal logs contain the error message: 'Failed to create session: Connection timed out' from pam_systemd

