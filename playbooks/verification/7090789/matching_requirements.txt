================================================================================
MATCHING REQUIREMENTS
KCS Article: Building custom execution image using ansible-builder fails due to conflict for  package  python39-jmespath on Ansible Automation Platform.
URL: https://access.redhat.com/solutions/7090789
================================================================================

1. Measure if OS version matches: Red Hat Enterprise Linux 8 (or compatible UBI 8)

2. Measure if Ansible Automation Platform version matches: 2.4

3. Measure if package python39-jmespath is installed and version matches: version 0.10.0-4.el8ap

4. Measure if package python3-jmespath is installed and version matches: version 0.9.0-11.el8 from the ubi-8-appstream-rpms repository

5. Measure if the ansible-builder command is being used to build a custom execution image

6. Measure if issue symptom is present: ansible-builder build process fails with a depsolve transaction error mentioning a conflict/obsoletes relationship between python39-jmespath and python3-jmespath

7. Measure if the specific error message is present: 'error: Could not depsolve transaction; 1 problem detected: Problem: installed package python39-jmespath-0.10.0-4.el8ap.noarch obsoletes python3-jmespath < 0.10.0-4.el8ap provided by python3-jmespath-0.9.0-11.el8.noarch from ubi-8-appstream-rpms- conflicting requests- problem with installed package python39-jmespath-0.10.0-4.el8ap.noarch'

