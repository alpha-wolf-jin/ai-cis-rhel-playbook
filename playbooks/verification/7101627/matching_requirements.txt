================================================================================
MATCHING REQUIREMENTS
KCS Article: Ansible-builder fails to build custom EE with error ‘Cannot find libsystemd or libsystemd-journal:’ when using image: registry.redhat.io/ansible-automation-platform-25/ee-supported-rhel8
URL: https://access.redhat.com/solutions/7101627
================================================================================

1. Measure if Ansible Automation Platform version matches: 2.5

2. Measure if ansible-builder is being used to build a custom Execution Environment (EE)

3. Measure if the base EE image used in the build matches: registry.redhat.io/ansible-automation-platform-25/ee-supported-rhel8:latest

4. Measure if the ansible-builder build process fails with an error during the 'python setup.py egg_info' stage

5. Measure if the build error output contains the specific string: 'Cannot find libsystemd or libsystemd-journal:'

6. Measure if the build error output contains the specific string: 'Package libsystemd was not found in the pkg-config search path.'

