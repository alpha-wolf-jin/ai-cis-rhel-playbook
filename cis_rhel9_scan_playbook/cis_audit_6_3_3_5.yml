---
- name: Data Collection for CIS 6.3.3.5
  hosts: all
  become: yes
  gather_facts: false
  vars:
    kcs_article: "CIS RHEL 9 Benchmark v2.0.0 checkpoint 6.3.3.5"
    req_1: "Check for syscall audit rule for sethostname and setdomainname (64-bit) in on-disk configuration"
    req_2: "Check for syscall audit rule for sethostname and setdomainname (32-bit) in on-disk configuration"
    req_3: "Check for file watch audit rule for /etc/issue in on-disk configuration"
    req_4: "Check for file watch audit rule for /etc/issue.net in on-disk configuration"
    req_5: "Check for file watch audit rule for /etc/hosts in on-disk configuration"
    req_6: "Check for file watch audit rule for /etc/hostname in on-disk configuration"
    req_7: "Check for file watch audit rule for /etc/sysconfig/network in on-disk configuration"
    req_8: "Check for file watch audit rule for /etc/sysconfig/network-scripts/ in on-disk configuration"
    req_9: "Check for file watch audit rule for /etc/NetworkManager in on-disk configuration"
    req_10: "Check for syscall audit rule for sethostname and setdomainname (64-bit) in running configuration"
    req_11: "Check for syscall audit rule for sethostname and setdomainname (32-bit) in running configuration"
    req_12: "Check for file watch audit rule for /etc/issue in running configuration"
    req_13: "Check for file watch audit rule for /etc/issue.net in running configuration"
    req_14: "Check for file watch audit rule for /etc/hosts in running configuration"
    req_15: "Check for file watch audit rule for /etc/hostname in running configuration"
    req_16: "Check for file watch audit rule for /etc/sysconfig/network in running configuration"
    req_17: "Check for file watch audit rule for /etc/sysconfig/network-scripts in running configuration"
    req_18: "Check for file watch audit rule for /etc/NetworkManager in running configuration"
    req_19: "OVERALL Verify: Ensure events that modify the system's network environment are collected"

  tasks:
    - name: Initialize data variables
      set_fact:
        task_1_name: "Not executed"
        task_1_cmd: "N/A"
        task_1_rc: -1
        task_2_name: "Not executed"
        task_2_cmd: "N/A"
        task_2_rc: -1
        task_3_name: "Not executed"
        task_3_cmd: "N/A"
        task_3_rc: -1
        task_4_name: "Not executed"
        task_4_cmd: "N/A"
        task_4_rc: -1
        task_5_name: "Not executed"
        task_5_cmd: "N/A"
        task_5_rc: -1
        task_6_name: "Not executed"
        task_6_cmd: "N/A"
        task_6_rc: -1
        task_7_name: "Not executed"
        task_7_cmd: "N/A"
        task_7_rc: -1
        task_8_name: "Not executed"
        task_8_cmd: "N/A"
        task_8_rc: -1
        task_9_name: "Not executed"
        task_9_cmd: "N/A"
        task_9_rc: -1
        task_10_name: "Not executed"
        task_10_cmd: "N/A"
        task_10_rc: -1
        task_11_name: "Not executed"
        task_11_cmd: "N/A"
        task_11_rc: -1
        task_12_name: "Not executed"
        task_12_cmd: "N/A"
        task_12_rc: -1
        task_13_name: "Not executed"
        task_13_cmd: "N/A"
        task_13_rc: -1
        task_14_name: "Not executed"
        task_14_cmd: "N/A"
        task_14_rc: -1
        task_15_name: "Not executed"
        task_15_cmd: "N/A"
        task_15_rc: -1
        task_16_name: "Not executed"
        task_16_cmd: "N/A"
        task_16_rc: -1
        task_17_name: "Not executed"
        task_17_cmd: "N/A"
        task_17_rc: -1
        task_18_name: "Not executed"
        task_18_cmd: "N/A"
        task_18_rc: -1

    # Req 1 - syscall 64-bit on-disk
    - name: Req 1 - Check syscall 64-bit on-disk
      shell: |
        awk '/^*-a *always, exit/ && /-F *arch=b64/ && /-S/ && (/sethostname/ || /setdomainname/) && (/skey= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
      args:
        executable: /bin/bash
      register: result_1
      ignore_errors: true
      changed_when: false

    - name: Store requirement 1 details
      set_fact:
        task_1_name: "Check syscall 64-bit on-disk"
        task_1_cmd: "awk '/^*-a *always, exit/ && /-F *arch=b64/ && /-S/ && (/sethostname/ || /setdomainname/) && (/skey= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules"
        task_1_rc: "{{ result_1.rc | default(-1) }}"
        data_1: "{{ result_1.stdout | default('') | trim }}"
        status_1: "{{ ('PASS' if ('-a always,exit -F arch=b64 -S sethostname,setdomainname -k system-locale' in (result_1.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_1: "{{ ('PASS when output contains -a always,exit -F arch=b64 -S sethostname,setdomainname -k system-locale or functionally equivalent rule with a key, FAIL otherwise' if ('-a always,exit -F arch=b64 -S sethostname,setdomainname -k system-locale' in (result_1.stdout | default(''))) else 'FAIL: Expected -a always,exit -F arch=b64 -S sethostname,setdomainname -k system-locale or equivalent') | trim }}"

    # Req 2 - syscall 32-bit on-disk
    - name: Req 2 - Check syscall 32-bit on-disk
      shell: |
        awk '/^*-a *always, exit/ && /-F *arch=b32/ && /-S/ && (/sethostname/ || /setdomainname/) && (/skey= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
      args:
        executable: /bin/bash
      register: result_2
      ignore_errors: true
      changed_when: false

    - name: Store requirement 2 details
      set_fact:
        task_2_name: "Check syscall 32-bit on-disk"
        task_2_cmd: "awk '/^*-a *always, exit/ && /-F *arch=b32/ && /-S/ && (/sethostname/ || /setdomainname/) && (/skey= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules"
        task_2_rc: "{{ result_2.rc | default(-1) }}"
        data_2: "{{ result_2.stdout | default('') | trim }}"
        status_2: "{{ ('PASS' if ('-a always,exit -F arch=b32 -S sethostname,setdomainname -k system-locale' in (result_2.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_2: "{{ ('PASS when output contains -a always,exit -F arch=b32 -S sethostname,setdomainname -k system-locale or functionally equivalent rule with a key, FAIL otherwise' if ('-a always,exit -F arch=b32 -S sethostname,setdomainname -k system-locale' in (result_2.stdout | default(''))) else 'FAIL: Expected -a always,exit -F arch=b32 -S sethostname,setdomainname -k system-locale or equivalent') | trim }}"

    # Req 3 - /etc/issue on-disk
    - name: Req 3 - Check /etc/issue on-disk
      shell: |
        awk '/^ *-w/ && (/etc\/issue/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
      args:
        executable: /bin/bash
      register: result_3
      ignore_errors: true
      changed_when: false

    - name: Store requirement 3 details
      set_fact:
        task_3_name: "Check /etc/issue on-disk"
        task_3_cmd: |
          awk '/^ *-w/ && (/etc\/issue/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
        task_3_rc: "{{ result_3.rc | default(-1) }}"
        data_3: "{{ result_3.stdout | default('') | trim }}"
        status_3: "{{ ('PASS' if ('-w /etc/issue -p wa -k system-locale' in (result_3.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_3: "{{ ('PASS when output contains -w /etc/issue -p wa -k system-locale or functionally equivalent rule with a key, FAIL otherwise' if ('-w /etc/issue -p wa -k system-locale' in (result_3.stdout | default(''))) else 'FAIL: Expected -w /etc/issue -p wa -k system-locale or equivalent') | trim }}"

    # Req 4 - /etc/issue.net on-disk
    - name: Req 4 - Check /etc/issue.net on-disk
      shell: |
        awk '/^ *-w/ && (/etc\/issue.net/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
      args:
        executable: /bin/bash
      register: result_4
      ignore_errors: true
      changed_when: false

    - name: Store requirement 4 details
      set_fact:
        task_4_name: "Check /etc/issue.net on-disk"
        task_4_cmd: |
          awk '/^ *-w/ && (/etc\/issue.net/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
        task_4_rc: "{{ result_4.rc | default(-1) }}"
        data_4: "{{ result_4.stdout | default('') | trim }}"
        status_4: "{{ ('PASS' if ('-w /etc/issue.net -p wa -k system-locale' in (result_4.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_4: "{{ ('PASS when output contains -w /etc/issue.net -p wa -k system-locale or functionally equivalent rule with a key, FAIL otherwise' if ('-w /etc/issue.net -p wa -k system-locale' in (result_4.stdout | default(''))) else 'FAIL: Expected -w /etc/issue.net -p wa -k system-locale or equivalent') | trim }}"

    # Req 5 - /etc/hosts on-disk
    - name: Req 5 - Check /etc/hosts on-disk
      shell: |
        awk '/^ *-w/ && (/etc\/hosts/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
      args:
        executable: /bin/bash
      register: result_5
      ignore_errors: true
      changed_when: false

    - name: Store requirement 5 details
      set_fact:
        task_5_name: "Check /etc/hosts on-disk"
        task_5_cmd: |
          awk '/^ *-w/ && (/etc\/hosts/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
        task_5_rc: "{{ result_5.rc | default(-1) }}"
        data_5: "{{ result_5.stdout | default('') | trim }}"
        status_5: "{{ ('PASS' if ('-w /etc/hosts -p wa -k system-locale' in (result_5.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_5: "{{ ('PASS when output contains -w /etc/hosts -p wa -k system-locale or functionally equivalent rule with a key, FAIL otherwise' if ('-w /etc/hosts -p wa -k system-locale' in (result_5.stdout | default(''))) else 'FAIL: Expected -w /etc/hosts -p wa -k system-locale or equivalent') | trim }}"

    # Req 6 - /etc/hostname on-disk
    - name: Req 6 - Check /etc/hostname on-disk
      shell: |
        awk '/^ *-w/ && (/etc\/hostname/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
      args:
        executable: /bin/bash
      register: result_6
      ignore_errors: true
      changed_when: false

    - name: Store requirement 6 details
      set_fact:
        task_6_name: "Check /etc/hostname on-disk"
        task_6_cmd: |
          awk '/^ *-w/ && (/etc\/hostname/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
        task_6_rc: "{{ result_6.rc | default(-1) }}"
        data_6: "{{ result_6.stdout | default('') | trim }}"
        status_6: "{{ ('PASS' if ('-w /etc/hostname -p wa -k system-locale' in (result_6.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_6: "{{ ('PASS when output contains -w /etc/hostname -p wa -k system-locale or functionally equivalent rule with a key, FAIL otherwise' if ('-w /etc/hostname -p wa -k system-locale' in (result_6.stdout | default(''))) else 'FAIL: Expected -w /etc/hostname -p wa -k system-locale or equivalent') | trim }}"

    # Req 7 - /etc/sysconfig/network on-disk
    - name: Req 7 - Check /etc/sysconfig/network on-disk
      shell: |
        awk '/^ *-w/ && (/etc\/sysconfig\/network/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
      args:
        executable: /bin/bash
      register: result_7
      ignore_errors: true
      changed_when: false

    - name: Store requirement 7 details
      set_fact:
        task_7_name: "Check /etc/sysconfig/network on-disk"
        task_7_cmd: |
          awk '/^ *-w/ && (/etc\/sysconfig\/network/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
        task_7_rc: "{{ result_7.rc | default(-1) }}"
        data_7: "{{ result_7.stdout | default('') | trim }}"
        status_7: "{{ ('PASS' if ('-w /etc/sysconfig/network -p wa -k system-locale' in (result_7.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_7: "{{ ('PASS when output contains -w /etc/sysconfig/network -p wa -k system-locale or functionally equivalent rule with a key, FAIL otherwise' if ('-w /etc/sysconfig/network -p wa -k system-locale' in (result_7.stdout | default(''))) else 'FAIL: Expected -w /etc/sysconfig/network -p wa -k system-locale or equivalent') | trim }}"

    # Req 8 - /etc/sysconfig/network-scripts/ on-disk
    - name: Req 8 - Check /etc/sysconfig/network-scripts/ on-disk
      shell: |
        awk '/^ *-w/ && (/etc\/sysconfig\/network-scripts\//) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
      args:
        executable: /bin/bash
      register: result_8
      ignore_errors: true
      changed_when: false

    - name: Store requirement 8 details
      set_fact:
        task_8_name: "Check /etc/sysconfig/network-scripts/ on-disk"
        task_8_cmd: |
          awk '/^ *-w/ && (/etc\/sysconfig\/network-scripts\//) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
        task_8_rc: "{{ result_8.rc | default(-1) }}"
        data_8: "{{ result_8.stdout | default('') | trim }}"
        status_8: "{{ ('PASS' if ('-w /etc/sysconfig/network-scripts/ -p wa -k system-locale' in (result_8.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_8: "{{ ('PASS when output contains -w /etc/sysconfig/network-scripts/ -p wa -k system-locale or functionally equivalent rule with a key, FAIL otherwise' if ('-w /etc/sysconfig/network-scripts/ -p wa -k system-locale' in (result_8.stdout | default(''))) else 'FAIL: Expected -w /etc/sysconfig/network-scripts/ -p wa -k system-locale or equivalent') | trim }}"

    # Req 9 - /etc/NetworkManager on-disk
    - name: Req 9 - Check /etc/NetworkManager on-disk
      shell: |
        awk '/^ *-w/ && (/etc\/NetworkManager/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
      args:
        executable: /bin/bash
      register: result_9
      ignore_errors: true
      changed_when: false

    - name: Store requirement 9 details
      set_fact:
        task_9_name: "Check /etc/NetworkManager on-disk"
        task_9_cmd: |
          awk '/^ *-w/ && (/etc\/NetworkManager/) && / +-p *wa/ && (/ key= *[!-~]* *$/ || /-k *[!-~]* *$/)' /etc/audit/rules.d/*.rules
        task_9_rc: "{{ result_9.rc | default(-1) }}"
        data_9: "{{ result_9.stdout | default('') | trim }}"
        status_9: "{{ ('PASS' if ('-w /etc/NetworkManager -p wa -k system-locale' in (result_9.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_9: "{{ ('PASS when output contains -w /etc/NetworkManager -p wa -k system-locale or functionally equivalent rule with a key, FAIL otherwise' if ('-w /etc/NetworkManager -p wa -k system-locale' in (result_9.stdout | default(''))) else 'FAIL: Expected -w /etc/NetworkManager -p wa -k system-locale or equivalent') | trim }}"

    # Req 10 - syscall 64-bit running
    - name: Req 10 - Check syscall 64-bit running
      shell: |
        auditctl -l | awk '/^ *-a *always,exit/ &&/ -F *arch=b64/ &&/ -S/ &&(/sethostname/ ||/setdomainname/) &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
      args:
        executable: /bin/bash
      register: result_10
      ignore_errors: true
      changed_when: false

    - name: Store requirement 10 details
      set_fact:
        task_10_name: "Check syscall 64-bit running"
        task_10_cmd: "auditctl -l | awk '/^ *-a *always,exit/ &&/ -F *arch=b64/ &&/ -S/ &&(/sethostname/ ||/setdomainname/) &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'"
        task_10_rc: "{{ result_10.rc | default(-1) }}"
        data_10: "{{ result_10.stdout | default('') | trim }}"
        status_10: "{{ ('PASS' if ('-a always,exit -F arch=b64 -S sethostname,setdomainname -F key=system-locale' in (result_10.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_10: "{{ ('PASS when output contains -a always,exit -F arch=b64 -S sethostname,setdomainname -F key=system-locale or functionally equivalent loaded rule with a key, FAIL otherwise' if ('-a always,exit -F arch=b64 -S sethostname,setdomainname -F key=system-locale' in (result_10.stdout | default(''))) else 'FAIL: Expected -a always,exit -F arch=b64 -S sethostname,setdomainname -F key=system-locale or equivalent') | trim }}"

    # Req 11 - syscall 32-bit running
    - name: Req 11 - Check syscall 32-bit running
      shell: |
        auditctl -l | awk '/^ *-a *always,exit/ &&/ -F *arch=b32/ &&/ -S/ &&(/sethostname/ ||/setdomainname/) &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
      args:
        executable: /bin/bash
      register: result_11
      ignore_errors: true
      changed_when: false

    - name: Store requirement 11 details
      set_fact:
        task_11_name: "Check syscall 32-bit running"
        task_11_cmd: "auditctl -l | awk '/^ *-a *always,exit/ &&/ -F *arch=b32/ &&/ -S/ &&(/sethostname/ ||/setdomainname/) &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'"
        task_11_rc: "{{ result_11.rc | default(-1) }}"
        data_11: "{{ result_11.stdout | default('') | trim }}"
        status_11: "{{ ('PASS' if ('-a always,exit -F arch=b32 -S sethostname,setdomainname -F key=system-locale' in (result_11.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_11: "{{ ('PASS when output contains -a always,exit -F arch=b32 -S sethostname,setdomainname -F key=system-locale or functionally equivalent loaded rule with a key, FAIL otherwise' if ('-a always,exit -F arch=b32 -S sethostname,setdomainname -F key=system-locale' in (result_11.stdout | default(''))) else 'FAIL: Expected -a always,exit -F arch=b32 -S sethostname,setdomainname -F key=system-locale or equivalent') | trim }}"

    # Req 12 - /etc/issue running
    - name: Req 12 - Check /etc/issue running
      shell: |
        auditctl -l | awk '/^ *-w/ &&(/etc\/issue/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
      args:
        executable: /bin/bash
      register: result_12
      ignore_errors: true
      changed_when: false

    - name: Store requirement 12 details
      set_fact:
        task_12_name: "Check /etc/issue running"
        task_12_cmd: |
          auditctl -l | awk '/^ *-w/ &&(/etc\/issue/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
        task_12_rc: "{{ result_12.rc | default(-1) }}"
        data_12: "{{ result_12.stdout | default('') | trim }}"
        status_12: "{{ ('PASS' if ('-w /etc/issue -p wa -k system-locale' in (result_12.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_12: "{{ ('PASS when output contains -w /etc/issue -p wa -k system-locale or functionally equivalent loaded rule with a key, FAIL otherwise' if ('-w /etc/issue -p wa -k system-locale' in (result_12.stdout | default(''))) else 'FAIL: Expected -w /etc/issue -p wa -k system-locale or equivalent') | trim }}"

    # Req 13 - /etc/issue.net running
    - name: Req 13 - Check /etc/issue.net running
      shell: |
        auditctl -l | awk '/^ *-w/ &&(/etc\/issue.net/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
      args:
        executable: /bin/bash
      register: result_13
      ignore_errors: true
      changed_when: false

    - name: Store requirement 13 details
      set_fact:
        task_13_name: "Check /etc/issue.net running"
        task_13_cmd: |
          auditctl -l | awk '/^ *-w/ &&(/etc\/issue.net/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
        task_13_rc: "{{ result_13.rc | default(-1) }}"
        data_13: "{{ result_13.stdout | default('') | trim }}"
        status_13: "{{ ('PASS' if ('-w /etc/issue.net -p wa -k system-locale' in (result_13.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_13: "{{ ('PASS when output contains -w /etc/issue.net -p wa -k system-locale or functionally equivalent loaded rule with a key, FAIL otherwise' if ('-w /etc/issue.net -p wa -k system-locale' in (result_13.stdout | default(''))) else 'FAIL: Expected -w /etc/issue.net -p wa -k system-locale or equivalent') | trim }}"

    # Req 14 - /etc/hosts running
    - name: Req 14 - Check /etc/hosts running
      shell: |
        auditctl -l | awk '/^ *-w/ &&(/etc\/hosts/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
      args:
        executable: /bin/bash
      register: result_14
      ignore_errors: true
      changed_when: false

    - name: Store requirement 14 details
      set_fact:
        task_14_name: "Check /etc/hosts running"
        task_14_cmd: |
          auditctl -l | awk '/^ *-w/ &&(/etc\/hosts/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
        task_14_rc: "{{ result_14.rc | default(-1) }}"
        data_14: "{{ result_14.stdout | default('') | trim }}"
        status_14: "{{ ('PASS' if ('-w /etc/hosts -p wa -k system-locale' in (result_14.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_14: "{{ ('PASS when output contains -w /etc/hosts -p wa -k system-locale or functionally equivalent loaded rule with a key, FAIL otherwise' if ('-w /etc/hosts -p wa -k system-locale' in (result_14.stdout | default(''))) else 'FAIL: Expected -w /etc/hosts -p wa -k system-locale or equivalent') | trim }}"

    # Req 15 - /etc/hostname running
    - name: Req 15 - Check /etc/hostname running
      shell: |
        auditctl -l | awk '/^ *-w/ &&(/etc\/hostname/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
      args:
        executable: /bin/bash
      register: result_15
      ignore_errors: true
      changed_when: false

    - name: Store requirement 15 details
      set_fact:
        task_15_name: "Check /etc/hostname running"
        task_15_cmd: |
          auditctl -l | awk '/^ *-w/ &&(/etc\/hostname/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
        task_15_rc: "{{ result_15.rc | default(-1) }}"
        data_15: "{{ result_15.stdout | default('') | trim }}"
        status_15: "{{ ('PASS' if ('-w /etc/hostname -p wa -k system-locale' in (result_15.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_15: "{{ ('PASS when output contains -w /etc/hostname -p wa -k system-locale or functionally equivalent loaded rule with a key, FAIL otherwise' if ('-w /etc/hostname -p wa -k system-locale' in (result_15.stdout | default(''))) else 'FAIL: Expected -w /etc/hostname -p wa -k system-locale or equivalent') | trim }}"

    # Req 16 - /etc/sysconfig/network running
    - name: Req 16 - Check /etc/sysconfig/network running
      shell: |
        auditctl -l | awk '/^ *-w/ &&(/etc\/sysconfig\/network/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
      args:
        executable: /bin/bash
      register: result_16
      ignore_errors: true
      changed_when: false

    - name: Store requirement 16 details
      set_fact:
        task_16_name: "Check /etc/sysconfig/network running"
        task_16_cmd: |
          auditctl -l | awk '/^ *-w/ &&(/etc\/sysconfig\/network/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
        task_16_rc: "{{ result_16.rc | default(-1) }}"
        data_16: "{{ result_16.stdout | default('') | trim }}"
        status_16: "{{ ('PASS' if ('-w /etc/sysconfig/network -p wa -k system-locale' in (result_16.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_16: "{{ ('PASS when output contains -w /etc/sysconfig/network -p wa -k system-locale or functionally equivalent loaded rule with a key, FAIL otherwise' if ('-w /etc/sysconfig/network -p wa -k system-locale' in (result_16.stdout | default(''))) else 'FAIL: Expected -w /etc/sysconfig/network -p wa -k system-locale or equivalent') | trim }}"

    # Req 17 - /etc/sysconfig/network-scripts running
    - name: Req 17 - Check /etc/sysconfig/network-scripts running
      shell: |
        auditctl -l | awk '/^ *-w/ &&(/etc\/sysconfig\/network-scripts/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
      args:
        executable: /bin/bash
      register: result_17
      ignore_errors: true
      changed_when: false

    - name: Store requirement 17 details
      set_fact:
        task_17_name: "Check /etc/sysconfig/network-scripts running"
        task_17_cmd: |
          auditctl -l | awk '/^ *-w/ &&(/etc\/sysconfig\/network-scripts/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
        task_17_rc: "{{ result_17.rc | default(-1) }}"
        data_17: "{{ result_17.stdout | default('') | trim }}"
        status_17: "{{ ('PASS' if ('-w /etc/sysconfig/network-scripts -p wa -k system-locale' in (result_17.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_17: "{{ ('PASS when output contains -w /etc/sysconfig/network-scripts -p wa -k system-locale or functionally equivalent loaded rule with a key, FAIL otherwise' if ('-w /etc/sysconfig/network-scripts -p wa -k system-locale' in (result_17.stdout | default(''))) else 'FAIL: Expected -w /etc/sysconfig/network-scripts -p wa -k system-locale or equivalent') | trim }}"

    # Req 18 - /etc/NetworkManager running
    - name: Req 18 - Check /etc/NetworkManager running
      shell: |
        auditctl -l | awk '/^ *-w/ &&(/etc\/NetworkManager/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
      args:
        executable: /bin/bash
      register: result_18
      ignore_errors: true
      changed_when: false

    - name: Store requirement 18 details
      set_fact:
        task_18_name: "Check /etc/NetworkManager running"
        task_18_cmd: |
          auditctl -l | awk '/^ *-w/ &&(/etc\/NetworkManager/) &&/ +-p *wa/ &&(/ key= *[!-~]* *$/||/ -k *[!-~]* *$/)'
        task_18_rc: "{{ result_18.rc | default(-1) }}"
        data_18: "{{ result_18.stdout | default('') | trim }}"
        status_18: "{{ ('PASS' if ('-w /etc/NetworkManager -p wa -k system-locale' in (result_18.stdout | default(''))) else 'FAIL') | trim }}"
        rationale_18: "{{ ('PASS when output contains -w /etc/NetworkManager -p wa -k system-locale or functionally equivalent loaded rule with a key, FAIL otherwise' if ('-w /etc/NetworkManager -p wa -k system-locale' in (result_18.stdout | default(''))) else 'FAIL: Expected -w /etc/NetworkManager -p wa -k system-locale or equivalent') | trim }}"

    # Req 19 - Overall verification
    - name: Store requirement 19 details
      set_fact:
        status_19: >-
          {% set all_status = [
            status_1 | default('FAIL') | trim,
            status_2 | default('FAIL') | trim,
            status_3 | default('FAIL') | trim,
            status_4 | default('FAIL') | trim,
            status_5 | default('FAIL') | trim,
            status_6 | default('FAIL') | trim,
            status_7 | default('FAIL') | trim,
            status_8 | default('FAIL') | trim,
            status_9 | default('FAIL') | trim,
            status_10 | default('FAIL') | trim,
            status_11 | default('FAIL') | trim,
            status_12 | default('FAIL') | trim,
            status_13 | default('FAIL') | trim,
            status_14 | default('FAIL') | trim,
            status_15 | default('FAIL') | trim,
            status_16 | default('FAIL') | trim,
            status_17 | default('FAIL') | trim,
            status_18 | default('FAIL') | trim
          ] %}
          {% if 'FAIL' in all_status %}
            FAIL
          {% else %}
            PASS
          {% endif %}
        rationale_19: "{{ ('PASS when all 18 individual requirements pass, FAIL otherwise' if (status_19 | default('FAIL') | trim == 'PASS') else 'FAIL: One or more individual requirements failed') | trim }}"

    - name: Generate compliance report
      debug:
        msg:
          - "========================================================"
          - "        COMPLIANCE REPORT - CIS 6.3.3.5"
          - "========================================================"
          - "Reference: CIS RHEL 9 Benchmark v2.0.0 checkpoint 6.3.3.5"
          - "========================================================"
          - ""
          - "REQUIREMENT 1 - {{ req_1 }}:"
          - "  Task: {{ task_1_name | default('Task not recorded') }}"
          - "  Command: {{ task_1_cmd | default('N/A') }}"
          - "  Exit code: {{ task_1_rc | default(-1) }}"
          - "  Data: {{ data_1 | default('') | trim }}"
          - "  Status: {{ status_1 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_1 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 2 - {{ req_2 }}:"
          - "  Task: {{ task_2_name | default('Task not recorded') }}"
          - "  Command: {{ task_2_cmd | default('N/A') }}"
          - "  Exit code: {{ task_2_rc | default(-1) }}"
          - "  Data: {{ data_2 | default('') | trim }}"
          - "  Status: {{ status_2 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_2 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 3 - {{ req_3 }}:"
          - "  Task: {{ task_3_name | default('Task not recorded') }}"
          - "  Command: {{ task_3_cmd | default('N/A') }}"
          - "  Exit code: {{ task_3_rc | default(-1) }}"
          - "  Data: {{ data_3 | default('') | trim }}"
          - "  Status: {{ status_3 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_3 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 4 - {{ req_4 }}:"
          - "  Task: {{ task_4_name | default('Task not recorded') }}"
          - "  Command: {{ task_4_cmd | default('N/A') }}"
          - "  Exit code: {{ task_4_rc | default(-1) }}"
          - "  Data: {{ data_4 | default('') | trim }}"
          - "  Status: {{ status_4 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_4 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 5 - {{ req_5 }}:"
          - "  Task: {{ task_5_name | default('Task not recorded') }}"
          - "  Command: {{ task_5_cmd | default('N/A') }}"
          - "  Exit code: {{ task_5_rc | default(-1) }}"
          - "  Data: {{ data_5 | default('') | trim }}"
          - "  Status: {{ status_5 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_5 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 6 - {{ req_6 }}:"
          - "  Task: {{ task_6_name | default('Task not recorded') }}"
          - "  Command: {{ task_6_cmd | default('N/A') }}"
          - "  Exit code: {{ task_6_rc | default(-1) }}"
          - "  Data: {{ data_6 | default('') | trim }}"
          - "  Status: {{ status_6 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_6 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 7 - {{ req_7 }}:"
          - "  Task: {{ task_7_name | default('Task not recorded') }}"
          - "  Command: {{ task_7_cmd | default('N/A') }}"
          - "  Exit code: {{ task_7_rc | default(-1) }}"
          - "  Data: {{ data_7 | default('') | trim }}"
          - "  Status: {{ status_7 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_7 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 8 - {{ req_8 }}:"
          - "  Task: {{ task_8_name | default('Task not recorded') }}"
          - "  Command: {{ task_8_cmd | default('N/A') }}"
          - "  Exit code: {{ task_8_rc | default(-1) }}"
          - "  Data: {{ data_8 | default('') | trim }}"
          - "  Status: {{ status_8 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_8 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 9 - {{ req_9 }}:"
          - "  Task: {{ task_9_name | default('Task not recorded') }}"
          - "  Command: {{ task_9_cmd | default('N/A') }}"
          - "  Exit code: {{ task_9_rc | default(-1) }}"
          - "  Data: {{ data_9 | default('') | trim }}"
          - "  Status: {{ status_9 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_9 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 10 - {{ req_10 }}:"
          - "  Task: {{ task_10_name | default('Task not recorded') }}"
          - "  Command: {{ task_10_cmd | default('N/A') }}"
          - "  Exit code: {{ task_10_rc | default(-1) }}"
          - "  Data: {{ data_10 | default('') | trim }}"
          - "  Status: {{ status_10 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_10 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 11 - {{ req_11 }}:"
          - "  Task: {{ task_11_name | default('Task not recorded') }}"
          - "  Command: {{ task_11_cmd | default('N/A') }}"
          - "  Exit code: {{ task_11_rc | default(-1) }}"
          - "  Data: {{ data_11 | default('') | trim }}"
          - "  Status: {{ status_11 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_11 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 12 - {{ req_12 }}:"
          - "  Task: {{ task_12_name | default('Task not recorded') }}"
          - "  Command: {{ task_12_cmd | default('N/A') }}"
          - "  Exit code: {{ task_12_rc | default(-1) }}"
          - "  Data: {{ data_12 | default('') | trim }}"
          - "  Status: {{ status_12 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_12 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 13 - {{ req_13 }}:"
          - "  Task: {{ task_13_name | default('Task not recorded') }}"
          - "  Command: {{ task_13_cmd | default('N/A') }}"
          - "  Exit code: {{ task_13_rc | default(-1) }}"
          - "  Data: {{ data_13 | default('') | trim }}"
          - "  Status: {{ status_13 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_13 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 14 - {{ req_14 }}:"
          - "  Task: {{ task_14_name | default('Task not recorded') }}"
          - "  Command: {{ task_14_cmd | default('N/A') }}"
          - "  Exit code: {{ task_14_rc | default(-1) }}"
          - "  Data: {{ data_14 | default('') | trim }}"
          - "  Status: {{ status_14 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_14 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 15 - {{ req_15 }}:"
          - "  Task: {{ task_15_name | default('Task not recorded') }}"
          - "  Command: {{ task_15_cmd | default('N/A') }}"
          - "  Exit code: {{ task_15_rc | default(-1) }}"
          - "  Data: {{ data_15 | default('') | trim }}"
          - "  Status: {{ status_15 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_15 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 16 - {{ req_16 }}:"
          - "  Task: {{ task_16_name | default('Task not recorded') }}"
          - "  Command: {{ task_16_cmd | default('N/A') }}"
          - "  Exit code: {{ task_16_rc | default(-1) }}"
          - "  Data: {{ data_16 | default('') | trim }}"
          - "  Status: {{ status_16 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_16 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 17 - {{ req_17 }}:"
          - "  Task: {{ task_17_name | default('Task not recorded') }}"
          - "  Command: {{ task_17_cmd | default('N/A') }}"
          - "  Exit code: {{ task_17_rc | default(-1) }}"
          - "  Data: {{ data_17 | default('') | trim }}"
          - "  Status: {{ status_17 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_17 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 18 - {{ req_18 }}:"
          - "  Task: {{ task_18_name | default('Task not recorded') }}"
          - "  Command: {{ task_18_cmd | default('N/A') }}"
          - "  Exit code: {{ task_18_rc | default(-1) }}"
          - "  Data: {{ data_18 | default('') | trim }}"
          - "  Status: {{ status_18 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_18 | default('Not evaluated') | trim }}"
          - ""
          - "REQUIREMENT 19 - {{ req_19 }}:"
          - "  Status: {{ status_19 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_19 | default('Not evaluated') | trim }}"
          - ""
          - "========================================================"
          - "OVERALL COMPLIANCE:"
          - "  Result: {{ status_19 | default('UNKNOWN') | trim }}"
          - "  Rationale: {{ rationale_19 | default('Not evaluated') | trim }}"
          - "========================================================"
